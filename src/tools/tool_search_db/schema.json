{
  "tool_name": "tool_search_db",
  "description": "Search for Brazilian investment funds in the database using structured criteria. Returns a list of funds with their metadata.",
  "input_schema": {
    "criteria": {
      "type": "object",
      "description": "Structured search criteria for funds",
      "properties": {
        "fund_legal_name": {
          "type": "string",
          "nullable": true,
          "description": "Legal name of the fund to search for (partial match supported)"
        },
        "fund_type": {
          "type": "string",
          "nullable": true,
          "description": "Fund type (FI, FIP, FIDC, FII, ETF, CLASSES - FIF)"
        },
        "investment_class": {
          "type": "string",
          "nullable": true,
          "description": "Investment class (Multimercado, Ações, Renda Fixa, etc.)"
        },
        "fund_of_funds": {
          "type": "boolean",
          "nullable": true,
          "description": "Whether to filter for fund of funds"
        }
      }
    },
    "limit": {
      "type": "integer",
      "default": 10,
      "description": "Maximum number of results to return"
    }
  },
  "output_schema": {
    "type": "array",
    "description": "List of funds matching the search criteria",
    "items": {
      "type": "object",
      "properties": {
        "legal_name": {
          "type": "string",
          "description": "Legal name of the fund"
        },
        "cnpj": {
          "type": "string",
          "description": "CNPJ identifier of the fund"
        },
        "fund_type": {
          "type": "string",
          "nullable": true,
          "description": "Fund type (FI, FIP, FIDC, FII, ETF, etc.)"
        },
        "investment_class": {
          "type": "string",
          "nullable": true,
          "description": "Investment class (Multimercado, Ações, Renda Fixa, etc.)"
        }
      },
      "required": ["legal_name", "cnpj", "fund_type", "investment_class"]
    }
  }
}
